# .readthedocs.yml
version: 2

build:
  os: ubuntu-20.04
  tools:
    python: "3.9"
  
python:
  install:
  - method: pip
    path: .
    extra_requirements:
    - doc
    
sphinx:
  configuration: docs/conf.py

# python:
#   install:
#     - requirements: docs/requirements.txt
